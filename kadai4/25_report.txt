void swap(int x, int y)
{
    int z;
 
    z = x;
    x = y;
    y = z;
}


という形でswap関数を作ってswap(x, y)として使うと、xとyの数字の中身が関数に渡される。
なので関数内でx, yに代入しても、それはmain関数内のx,yとは別の変数になっているので、書き換えられない。
ポインタを使ってswap関数にメモリのアドレス自体を渡すべき。
